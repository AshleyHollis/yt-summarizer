[project]
name = "yt-summarizer-mcp"
version = "0.1.0"
description = "MCP server for yt-summarizer API"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
include = ["server.py"]

[dependency-groups]
dev = [
    "ruff>=0.15.9",
]
