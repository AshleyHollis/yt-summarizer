---
# ServiceAccount for preview environments
# Uses ClusterSecretStore for secrets access (no workload identity needed)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: yt-summarizer-sa
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
  labels:
    app.kubernetes.io/name: yt-summarizer
    app.kubernetes.io/component: serviceaccount
