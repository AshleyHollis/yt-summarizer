# Delete the nginx Ingress - preview uses Gateway API HTTPRoute instead
# This patch removes the base api-ingress.yaml resource
$patch: delete
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-ingress
  namespace: yt-summarizer
