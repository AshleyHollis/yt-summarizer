# This file is a template for preview HTTPRoute patches
# It will be generated by scripts/ci/generate_preview_kustomization.py
# with the PR number substituted into hostnames and annotations

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: api-httproute
  annotations:
    external-dns.alpha.kubernetes.io/hostname: api-pr-{{PR_NUMBER}}.yt-summarizer.apps.ashleyhollis.com
spec:
  hostnames:
  - "api-pr-{{PR_NUMBER}}.yt-summarizer.apps.ashleyhollis.com"
