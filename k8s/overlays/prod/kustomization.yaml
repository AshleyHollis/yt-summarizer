# =============================================================================
# Production Overlay Kustomization
# =============================================================================
# Single production environment - auto-synced by Argo CD on merge to main
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: yt-summarizer

resources:
  - ../../base-app
  - priority-class.yaml

# Application resources only - SecretStore and ServiceAccount managed by eso-secretstore application

# Image tags - updated by deploy-prod.yml workflow on merge
# Using digest pinning for immutable deployments
images:
  - name: yt-summarizer-api
    newName: acrytsummprd.azurecr.io/yt-summarizer-api
    newTag: latest
  - name: yt-summarizer-workers
    newName: acrytsummprd.azurecr.io/yt-summarizer-workers
    newTag: latest

# Production-specific patches
patches:
  - path: patches/configmap-patch.yaml
  - path: patches/ingress-patch.yaml
  # Production resource requests/limits per deployment (split into per-target patches)
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: api
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: api
      spec:
        template:
          spec:
            containers:
              - name: api
                resources:
                  requests:
                    cpu: 25m
                    memory: 128Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: transcribe-worker
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: transcribe-worker
      spec:
        template:
          spec:
            containers:
              - name: worker
                resources:
                  requests:
                    cpu: 25m
                    memory: 128Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: summarize-worker
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: summarize-worker
      spec:
        template:
          spec:
            containers:
              - name: worker
                resources:
                  requests:
                    cpu: 25m
                    memory: 128Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: embed-worker
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: embed-worker
      spec:
        template:
          spec:
            containers:
              - name: worker
                resources:
                  requests:
                    cpu: 25m
                    memory: 128Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: relationships-worker
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: relationships-worker
      spec:
        template:
          spec:
            containers:
              - name: worker
                resources:
                  requests:
                    cpu: 25m
                    memory: 128Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi
  # Priority class patches for all deployments
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: api
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: api
      spec:
        template:
          spec:
            priorityClassName: production-critical
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: transcribe-worker
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: transcribe-worker
      spec:
        template:
          spec:
            priorityClassName: production-critical
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: summarize-worker
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: summarize-worker
      spec:
        template:
          spec:
            priorityClassName: production-critical
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: embed-worker
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: embed-worker
      spec:
        template:
          spec:
            priorityClassName: production-critical
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: relationships-worker
      namespace: yt-summarizer
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: relationships-worker
      spec:
        template:
          spec:
            priorityClassName: production-critical

labels:
  - pairs:
      environment: production
      app.kubernetes.io/part-of: yt-summarizer

commonAnnotations:
  deployment.kubernetes.io/environment: production
