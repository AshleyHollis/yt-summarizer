# Generated by update_prod_kustomization.sh on 2026-06-13 21:01:15 UTC
# Image Tag: sha-517d9f6


# =============================================================================
# OpenClaw Production Overlay Kustomization Template
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: yt-summarizer

resources:
  - ../prod
  - transcribe-worker-scaledobject.yaml

images:
  # The nested ../prod overlay rewrites short image names to ACR names first.
  # Match those rewritten names here so OpenClaw production receives the new tag.
  - name: acrytsummprdci.azurecr.io/yt-summarizer-api
    newName: acrytsummprdci.azurecr.io/yt-summarizer-api
    newTag: sha-517d9f6
  - name: acrytsummprdci.azurecr.io/yt-summarizer-workers
    newName: acrytsummprdci.azurecr.io/yt-summarizer-workers
    newTag: sha-517d9f6

patches:
  - target:
      group: external-secrets.io
      version: v1beta1
      kind: SecretStore
      name: azure-keyvault
    patch: |-
      apiVersion: external-secrets.io/v1beta1
      kind: SecretStore
      metadata:
        name: azure-keyvault
      $patch: delete

  - target:
      group: external-secrets.io
      version: v1beta1
      kind: ExternalSecret
    patch: |-
      - op: replace
        path: /apiVersion
        value: external-secrets.io/v1
      - op: replace
        path: /spec/secretStoreRef/kind
        value: ClusterSecretStore
      - op: replace
        path: /spec/secretStoreRef/name
        value: azure-keyvault-cluster

  - target:
      version: v1
      kind: ServiceAccount
      name: yt-summarizer-sa
    patch: |-
      - op: add
        path: /imagePullSecrets
        value:
          - name: acr-pull-secret
      - op: remove
        path: /metadata/annotations/azure.workload.identity~1client-id
      - op: remove
        path: /metadata/annotations/azure.workload.identity~1tenant-id
      - op: remove
        path: /metadata/labels/azure.workload.identity~1use

  - target:
      group: gateway.networking.k8s.io
      version: v1
      kind: HTTPRoute
      name: api-httproute
    patch: |-
      - op: replace
        path: /spec/parentRefs/0/sectionName
        value: http-yt-summarizer-prod
      - op: replace
        path: /spec/hostnames/0
        value: api-ytsummarizer.ashleyhollis.com
      - op: remove
        path: /metadata/annotations/external-dns.alpha.kubernetes.io~1hostname

  - target:
      group: apps
      version: v1
      kind: Deployment
      name: transcribe-worker
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: transcribe-worker
      spec:
        template:
          spec:
            containers:
              - name: worker
                env:
                  - name: QUEUE_BATCH_SIZE
                    value: "2"
                  - name: PROXY_MAX_CONCURRENCY
                    value: "2"

labels:
  - pairs:
      app.kubernetes.io/platform: openclaw

commonAnnotations:
  deployment.kubernetes.io/platform: openclaw
