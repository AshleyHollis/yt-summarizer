## Kane — 2026-04-06

### Task
Hardened auth setup and fixed stale LIVE_PROCESSING comment.

### Outcome
- **Commit:** `test(e2e): harden auth setup + fix stale LIVE_PROCESSING comment` (`3142afe8`)
- **Status:** ✅ Complete
- **Key Changes:**
  1. `apps/web/e2e/auth.setup.ts` now throws when admin role claim is absent (was warning and silently saving broken admin.json)
  2. `admin.json` only saved when role is confirmed `admin`
  3. Error handling improved — catch block re-throws instead of swallowing errors
  4. `playwright.config.ts` stale comment fixed — LIVE_PROCESSING IS set in preview workflows

### Key Insight
Silently saving broken `admin.json` causes all RBAC tests to skip with misleading "no admin.json found" messages. Strict role validation prevents cascading failures.

### Pattern for Future
Guard storageState save inside role-confirmation branch. Never silently save partial/broken state — fail fast.
