## Parker — 2026-04-06

### Task 1
Identify missing Auth0 environment variables in preview.yml E2E workflow.

### Outcome 1
- **Commit:** `ci(e2e): add missing Auth0 env vars to preview.yml E2E step` (`3a09dbf5`)
- **Status:** ✅ Complete
- **Finding:** `preview.yml` lacked `AUTH0_SECRET`, `AUTH0_BASE_URL`, `AUTH0_TEST_EMAIL`, `AUTH0_TEST_PASSWORD` that `preview-e2e.yml` had
- **Failure Chain Identified:**
  1. Auth setup fails due to missing env vars
  2. 5 quick test failures occur
  3. `maxFailures=5` aborts the run
  4. LIVE_PROCESSING tests never execute
  5. Appears "by design" but actually infrastructure misconfiguration

### Verification
- LIVE_PROCESSING IS correctly forwarded through shared-infra composite action (confirmed via code trace)
- Issue was incomplete env var mirror, not workflow logic

### Task 2
Evaluate and adjust MAX_FAILURES threshold for E2E flakes.

### Outcome 2
- **Status:** Pending commit
- **Change:** MAX_FAILURES raised 5 → 10
- **Rationale:** LLM-based test generation causes non-deterministic flakes in initial setup before LIVE_PROCESSING tests execute. 5 is too low.

### Pattern for Future
- Always cross-check both `preview.yml` and `preview-e2e.yml` for env var coverage when debugging E2E failures
- MAX_FAILURES=5 is too aggressive. Baseline should be 10+ when LLM generation is involved
